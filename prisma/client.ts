// Centralize Prisma imports so the later generator/output switch stays local.
export { Prisma, PrismaClient } from '@prisma/client'
export type { Match, PlayerThrow, Tournament } from '@prisma/client'
