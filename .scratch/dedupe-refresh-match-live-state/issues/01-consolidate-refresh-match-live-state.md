Status: completed

> _This was generated by AI during triage._

## What to build

Two near-identical copies of `refreshMatchLiveState` exist:

- `app/lib/match-live-state/refresh.ts` — the canonical implementation, imported by `app/lib/match-live-state/index.ts` and exercised by both the unit suite and the integration suite.
- `app/lib/data/match-operations.ts` — a copy with the same body, reached only via the barrel re-export in `app/lib/data/index.ts`.

No route, component, server action, or API handler imports `refreshMatchLiveState` from `@/app/lib/data`. The only consumers of the data-barrel's copy are the smoke tests under `app/lib/data/__tests__/match-operations.test.ts`, which assert the export exists and the function is callable.

Consolidate so there is one implementation and one source of truth:

- Delete the `refreshMatchLiveState` function body from `app/lib/data/match-operations.ts`.
- Update the `app/lib/data/index.ts` re-export to source `refreshMatchLiveState` from `app/lib/match-live-state` (matching the existing `refreshMatchLiveStateFromRepo` re-export already in that file).
- Update or remove the `app/lib/data/__tests__/match-operations.test.ts` smoke tests for `refreshMatchLiveState` so they exercise the consolidated path (or drop them if they no longer add coverage beyond what `app/__tests__/match-live-state.test.ts` and the integration suite already provide).
- Keep `getSyncedMatchLegState` in `match-operations.ts` — that function is used by `app/lib/data/queries.ts` and is unrelated to the duplication.

Background: the duplication is what hid the leg-2 starter bug from the previous fixer. They patched one copy, the route kept reading from the other. The unit test in `app/__tests__/match-live-state.test.ts` was importing the data barrel, so it passed against the wrong copy and gave false confidence.

## Acceptance criteria

- [x] `app/lib/data/match-operations.ts` no longer defines `refreshMatchLiveState`.
- [x] `app/lib/data/index.ts` re-exports `refreshMatchLiveState` sourced from `app/lib/match-live-state`. Only one binding named `refreshMatchLiveState` exists in that file (the `refreshMatchLiveStateFromRepo` alias is unrelated and may stay or be removed at the agent's discretion).
- [x] A `rg -n "function refreshMatchLiveState" app/` returns exactly one match (the canonical one).
- [x] A `rg -rn "from ['\"].*lib/data['\"]" app/ | grep -i refresh` returns no matches outside `__tests__/` directories.
- [x] `npm test` passes (all 276+ existing unit tests, including the three regression cases added in `app/__tests__/match-live-state.test.ts`).
- [x] `npm run lint` reports no new errors.
- [x] The `data/match-operations.test.ts` smoke tests are either updated to assert the consolidated export or removed if redundant.

## Blocked by

None - can start immediately.
