Status: needs-triage

> *This was generated by AI during triage.*

## What to build

Three tests in `dashboard-cache-revalidate.spec.ts` (lines 84, 136, 194) have the same race: they call `await tablePage.goto('/tables/1')` followed immediately by `await tablePage.getByTestId(\`start-player-${playerAId}\`).click()`, and then `await expect(tablePage.getByRole('button', { name: 'UNDO' })).toBeVisible()`. In production, the page renders faster than in dev, so the scoreboard hasn't mounted yet when the test starts interacting — the page snapshot at failure shows the table is still on the `/tables` listing, not on `/tables/1`.

The tests were passing in dev mode because the dev server's slow HMR / compile time masked the race. After the containerised test runner was switched to a production build, these tests fail deterministically (or sometimes retry-pass, so they show up as "flaky").

The right fix is to wait for a stable page signal that the scoreboard is mounted before interacting with it. The existing tests in this file already use `await page.getByText('First to play:').toBeVisible()` in `openStartedTableOne()` (`tests/ui/scoreboard.spec.ts:13`); the E2E tests should use an equivalent wait.

## Acceptance criteria

- [ ] All three failing tests at `dashboard-cache-revalidate.spec.ts:84, 136, 194` pass deterministically across at least 3 consecutive `docker compose -f docker-compose.test.yml run --rm test e2e` runs with no retries.
- [ ] No `page.waitForTimeout(...)` or other hardcoded sleeps are introduced.
- [ ] The wait is anchored to a real page-state signal (e.g. `await expect(tablePage.getByText('First to play:')).toBeVisible()` before the `start-player-` click, matching the pattern already used in the UI tests at `tests/ui/scoreboard.spec.ts:13`).
- [ ] The fix is applied consistently across all three tests in this file (not just one).
- [ ] The other tests in `match-flow.spec.ts` and the rest of the E2E suite are unaffected by the change.

## Blocked by

None - can start immediately.

## Background

Root cause analysis is in `tests/e2e/dashboard-cache-revalidate.spec.ts` at the `await tablePage.getByTestId(\`start-player-${playerAId}\`).click()` lines. The runner now uses `next start` (production build), so dev-mode timing no longer hides this race. The tests were passing accidentally in the previous dev-mode runner.
