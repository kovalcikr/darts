import { describe, expect, test, jest, beforeEach } from '@jest/globals';

// TODO: Add tests for playerThrow.ts. The tests were removed because of issues with mocking prisma transactions.
describe('playerThrow', () => {
    test('dummy test', () => {
        expect(true).toBe(true);
    });
});
